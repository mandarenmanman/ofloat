spin_manifest_version = 2

[application]
name = "spin-user-service"
version = "0.1.0"
description = "Go WASM user service with Dapr sidecar"

[[trigger.http]]
route = "/..."
component = "spin-user-service"

[component.spin-user-service]
source = "main.wasm"
allowed_outbound_hosts = [
    "http://127.0.0.1:3507",
]
[component.spin-user-service.build]
command = "tinygo build -target=wasip1 -buildmode=c-shared -no-debug -o main.wasm ."
watch = ["**/*.go", "go.mod"]
