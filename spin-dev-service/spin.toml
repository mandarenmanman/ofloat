spin_manifest_version = 2

[application]
name = "spin-dev-service"
version = "0.1.0"
description = "Go WASM device management service with Dapr sidecar"

[[trigger.http]]
route = "/..."
component = "spin-dev-service"

[component.spin-dev-service]
source = "main.wasm"
allowed_outbound_hosts = [
    "http://127.0.0.1:3516",
]
[component.spin-dev-service.build]
command = "tinygo build -target=wasip1 -buildmode=c-shared -no-debug -o main.wasm ."
watch = ["**/*.go", "go.mod"]
