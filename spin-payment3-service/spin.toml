spin_manifest_version = 2

[application]
name = "spin-payment3-service"
version = "0.1.0"
description = "Go WASM payment service with Dapr sidecar - state store & service invocation"

[[trigger.http]]
route = "/..."
component = "spin-payment3-service"

[component.spin-payment3-service]
source = "main.wasm"
allowed_outbound_hosts = [
    "http://127.0.0.1:3510",
]
[component.spin-payment3-service.build]
command = "tinygo build -target=wasip1 -buildmode=c-shared -no-debug -o main.wasm ."
watch = ["**/*.go", "go.mod"]
